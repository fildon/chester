{
  "source": "src/index.html",
  "targets": {
    "default": {
      "publicUrl": "./"
    }
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "dev": "parcel",
    "build": "parcel build"
  },
  "dependencies": {
    "chessground": "^9.1.1",
    "chessops": "^0.14.2"
  },
  "devDependencies": {
    "parcel": "^2.13.3"
  }
}
